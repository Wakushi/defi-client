/** Paramètres collatéral choisis dans l’UI pour `openTrade`. */
export type TradeCollateralSelection = {
  collateralAmountRaw: string;
  tokenAddress: string;
  decimals: number;
  symbol: string;
};
